%p#notice= notice
| 嘿嘿嘿嘿，我试试cap自动更新
%p
  %b Name:
  = @article.name
%p
  %b User:
  = @article.user_id
%p
  %b image:
  - @article.images.each_with_index do |image, index|
    = link_to image.filename, rails_blob_path(image, disposition: "image")
  -#= rails_blob_path(@article.images, disposition: "image")

= link_to 'Edit', edit_article_path(@article)
\|
= link_to 'Back', articles_path

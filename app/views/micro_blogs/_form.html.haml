= form_for @micro_blog do |f|
  - if @micro_blog.errors.any?
    #error_explanation
      %h2= "#{pluralize(@micro_blog.errors.count, "error")} prohibited this micro_blog from being saved:"
      %ul
        - @micro_blog.errors.full_messages.each do |message|
          %li= message

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :content
    = f.text_area :content
  .actions
    = f.submit 'Save'
